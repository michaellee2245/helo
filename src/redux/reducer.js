const initialState = {

    id: '',
    username: '',
    profilePic: '',

}

const reducer = (state = initialState, actions) => {
    switch(actions.type){
        default:
            return state
    }
}


export default reducer

